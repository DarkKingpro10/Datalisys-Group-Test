Inicialización Docker (placeholder)

Pasos:

1. Construir imágenes:

```powershell
docker compose build
```

2. Levantar servicios:

```powershell
docker compose up
```

Notas:
- Se han creado carpetas `frontend` y `backend` con Dockerfiles básicos.
- No se han añadido `package.json`, `requirements.txt` ni código fuente todavía — esto es solo para inicializar Docker.
